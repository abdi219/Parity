# Task 3: Interactive Dashboard UI

## Objective
Build the main dashboard in `web/src/app/page.tsx` adhering to `docs/RULES.md`.

## Requirements
- High-craft monochrome UI (zinc-950 background, subtle borders, zero emojis, zero neon glows).
- Input bar with "Inspect Auth Service (Demo)" instant button.
- Animated terminal stepper showing parsing progress.
- Drift Matrix showing side-by-side red vs green cards with exact line citations.
- FAQ accordion at the bottom explaining how DriftGuard works.